# redis-benchmark - Redis performance testing
tool_name: redis-benchmark
version: '1.0'
description: Redis performance testing
platform: database
category: database
defaults:
  accuracy_level: real-time
  freshness: live
  data_source: direct
capabilities:
  primary_capability:
    description: redis-benchmark primary capability
    patterns:
      execute:
        description: Execute redis-benchmark command
        typical_use_cases:
        - use redis-benchmark
        - redis-benchmark command
        - redis performance testing
        time_estimate_ms: '1500'
        cost_estimate: '2'
        complexity_score: 0.4
        scope: single_item
        completeness: complete
        policy:
          max_cost: 10
          requires_approval: false
          production_safe: true
          max_execution_time: 60
        preference_match:
          speed: 0.8
          accuracy: 0.9
          cost: 0.9
          complexity: 0.8
          completeness: 0.9
        required_inputs:
        - name: host
          type: string
          description: Target host
          validation: .*
metadata:
  author: OpsConductor Team
  created: '2025-10-03'
  updated: '2025-10-03'
  tags:
  - database
  - database
  - redis-benchmark
  documentation_url: https://docs.opsconductor.io/tools/redis-benchmark

# === EXAMPLES ===
examples:
  - name: "Execute"
    description: "Execute redis-benchmark command"
    inputs:
      host: "192.168.1.100"
    expected_time_ms: 1500
    expected_cost: 2
