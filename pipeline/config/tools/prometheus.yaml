description: Monitor system using prometheus
platform: linux
category: monitoring
defaults:
  accuracy_level: real-time
  freshness: live
  data_source: direct
dependencies:
- name: prometheus_agent
  type: service
  required: true
capabilities:
- capability_name: system_monitoring
  description: Monitor system metrics with prometheus
  patterns:
  - pattern_name: get_metrics
    description: Retrieve system metrics
    typical_use_cases:
    - Check system health
    - Monitor resources
    time_estimate_ms: '200'
    cost_estimate: '1'
    complexity_score: 0.3
    scope: single
    completeness: full
    limitations:
    - Requires agent running
    policy:
      max_cost: 5
      requires_approval: false
      production_safe: true
    preference_match:
      speed: 0.9
      accuracy: 0.9
      cost: 0.9
      complexity: 0.7
      completeness: 0.9
    required_inputs:
    - name: metric_type
      type: string
      required: true
      description: Type of metric to retrieve
    expected_outputs:
    - name: metrics
      type: object
      required: true
      description: Metric values
    - name: timestamp
      type: string
      required: true
      description: Collection time
    examples:
    - description: Get CPU metrics
      input: '{{metric_type: "cpu"}}'
      output: '{{metrics: {{usage: 45.2, cores: 8}}, timestamp: "2025-10-03T19:00:00Z"}}'
tool_name: prometheus
version: '1.0'
status: active
enabled: true
metadata:
  author: Tool Generator
  template: monitoring_tool
  tags:
  - monitoring-tool
created_by: Tool Generator

# === EXAMPLES ===
examples:
  - name: "Get CPU Metrics"
    description: "Retrieve CPU usage metrics from Prometheus"
    inputs:
      metric_type: "cpu"
    expected_time_ms: 200
    expected_cost: 1
  - name: "Get Memory Metrics"
    description: "Retrieve memory usage metrics from Prometheus"
    inputs:
      metric_type: "memory"
    expected_time_ms: 200
    expected_cost: 1
  - name: "Get Disk Metrics"
    description: "Retrieve disk usage metrics from Prometheus"
    inputs:
      metric_type: "disk"
    expected_time_ms: 200
    expected_cost: 1
