# ss - Display socket statistics (modern netstat)
tool_name: ss
execution_location: automation-service
version: '1.0'
description: Display socket statistics (modern netstat)
platform: linux
category: network
defaults:
  accuracy_level: real-time
  freshness: live
  data_source: direct
capabilities:
  network_monitoring:
    description: network_monitoring capability
    patterns:
      list_sockets:
        description: list_sockets pattern
        typical_use_cases:
        - list sockets
        - network connections
        - socket stats
        time_estimate_ms: '300'
        cost_estimate: '1'
        complexity_score: 0.2
        scope: single_item
        completeness: complete
        policy:
          max_cost: 10
          requires_approval: false
          production_safe: true
          max_execution_time: 60
        preference_match:
          speed: 0.8
          accuracy: 0.9
          cost: 0.9
          complexity: 0.8
          completeness: 0.9
        required_inputs:
        - name: host
          type: string
          description: Target host
          validation: .*
metadata:
  author: OpsConductor Team
  created: '2025-10-03'
  updated: '2025-10-03'
  tags:
  - linux
  - network
  - ss
  documentation_url: https://docs.opsconductor.io/tools/ss

# === EXAMPLES ===
examples:
  - name: "List Sockets"
    description: "list_sockets pattern"
    inputs:
      host: "192.168.1.100"
    expected_time_ms: 300
    expected_cost: 1
