# curl - Transfer data with URLs (HTTP, FTP, etc.)
tool_name: curl
version: '1.0'
description: Transfer data with URLs (HTTP, FTP, etc.)
platform: linux
category: network
defaults:
  accuracy_level: real-time
  freshness: live
  data_source: direct
capabilities:
  http_client:
    description: http_client capability
    patterns:
      http_request:
        description: http_request pattern
        typical_use_cases:
        - http request
        - api call
        - download file
        time_estimate_ms: 1000 + response_time
        cost_estimate: '1'
        complexity_score: 0.2
        scope: single_item
        completeness: complete
        policy:
          max_cost: 10
          requires_approval: false
          production_safe: true
          max_execution_time: 60
        preference_match:
          speed: 0.8
          accuracy: 0.9
          cost: 0.9
          complexity: 0.8
          completeness: 0.9
        required_inputs:
        - name: host
          type: string
          description: Target host
          validation: .*
metadata:
  author: OpsConductor Team
  created: '2025-10-03'
  updated: '2025-10-03'
  tags:
  - linux
  - network
  - curl
  documentation_url: https://docs.opsconductor.io/tools/curl

# === EXAMPLES ===
examples:
  - name: "Http Request"
    description: "http_request pattern"
    inputs:
      host: "192.168.1.100"
    expected_time_ms: 1000
    expected_cost: 1
