# ping - Test network connectivity (ICMP)
tool_name: ping
version: '1.0'
description: Test network connectivity (ICMP)
platform: linux
category: network
defaults:
  accuracy_level: real-time
  freshness: live
  data_source: direct
capabilities:
  network_testing:
    description: network_testing capability
    patterns:
      test_connectivity:
        description: test_connectivity pattern
        typical_use_cases:
        - ping host
        - test connectivity
        - check network
        time_estimate_ms: 1000 * count
        cost_estimate: '1'
        complexity_score: 0.1
        scope: single_item
        completeness: complete
        policy:
          max_cost: 10
          requires_approval: false
          production_safe: true
          max_execution_time: 60
        preference_match:
          speed: 0.8
          accuracy: 0.9
          cost: 0.9
          complexity: 0.8
          completeness: 0.9
        required_inputs:
        - name: host
          type: string
          description: Target host
          validation: .*
metadata:
  author: OpsConductor Team
  created: '2025-10-03'
  updated: '2025-10-03'
  tags:
  - linux
  - network
  - ping
  documentation_url: https://docs.opsconductor.io/tools/ping
