# asset-list - List all assets
tool_name: asset-list
version: '1.0'
description: List all assets
platform: custom
category: asset
defaults:
  accuracy_level: real-time
  freshness: live
  data_source: direct
capabilities:
  primary_capability:
    description: asset-list primary capability
    patterns:
      execute:
        description: Execute asset-list command
        typical_use_cases:
        - use asset-list
        - asset-list command
        - list all assets
        time_estimate_ms: '500'
        cost_estimate: '1'
        complexity_score: 0.2
        scope: single_item
        completeness: complete
        policy:
          max_cost: 10
          requires_approval: false
          production_safe: true
          max_execution_time: 60
        preference_match:
          speed: 0.8
          accuracy: 0.9
          cost: 0.9
          complexity: 0.8
          completeness: 0.9
        required_inputs:
        - name: host
          type: string
          description: Target host
          validation: .*
metadata:
  author: OpsConductor Team
  created: '2025-10-03'
  updated: '2025-10-03'
  tags:
  - custom
  - asset
  - asset-list
  documentation_url: https://docs.opsconductor.io/tools/asset-list
