# helm-install - Install Helm chart
tool_name: helm-install
execution_location: automation-service
version: '1.0'
description: Install Helm chart
platform: kubernetes
category: container
defaults:
  accuracy_level: real-time
  freshness: live
  data_source: direct
capabilities:
  primary_capability:
    description: helm-install primary capability
    patterns:
      execute:
        description: Execute helm-install command
        typical_use_cases:
        - use helm-install
        - helm-install command
        - install helm chart
        time_estimate_ms: '1000'
        cost_estimate: '2'
        complexity_score: 0.3
        scope: single_item
        completeness: complete
        policy:
          max_cost: 10
          requires_approval: false
          production_safe: true
          max_execution_time: 60
        preference_match:
          speed: 0.8
          accuracy: 0.9
          cost: 0.9
          complexity: 0.8
          completeness: 0.9
        required_inputs:
        - name: host
          type: string
          description: Target host
          validation: .*
metadata:
  author: OpsConductor Team
  created: '2025-10-03'
  updated: '2025-10-03'
  tags:
  - kubernetes
  - container
  - helm-install
  documentation_url: https://docs.opsconductor.io/tools/helm-install

# === EXAMPLES ===
examples:
  - name: "Execute"
    description: "Execute helm-install command"
    inputs:
      host: "192.168.1.100"
    expected_time_ms: 1000
    expected_cost: 2
