# docker-stats - Display container resource usage
tool_name: docker-stats
version: '1.0'
description: Display container resource usage
platform: container
category: container
defaults:
  accuracy_level: real-time
  freshness: live
  data_source: direct
capabilities:
  primary_capability:
    description: docker-stats primary capability
    patterns:
      execute:
        description: Execute docker-stats command
        typical_use_cases:
        - use docker-stats
        - docker-stats command
        - display container resource usage
        time_estimate_ms: '1000'
        cost_estimate: '2'
        complexity_score: 0.3
        scope: single_item
        completeness: complete
        policy:
          max_cost: 10
          requires_approval: false
          production_safe: true
          max_execution_time: 60
        preference_match:
          speed: 0.8
          accuracy: 0.9
          cost: 0.9
          complexity: 0.8
          completeness: 0.9
        required_inputs:
        - name: host
          type: string
          description: Target host
          validation: .*
metadata:
  author: OpsConductor Team
  created: '2025-10-03'
  updated: '2025-10-03'
  tags:
  - container
  - container
  - docker-stats
  documentation_url: https://docs.opsconductor.io/tools/docker-stats
