# kubens - Switch between Kubernetes namespaces
tool_name: kubens
version: '1.0'
description: Switch between Kubernetes namespaces
platform: kubernetes
category: container
defaults:
  accuracy_level: real-time
  freshness: live
  data_source: direct
capabilities:
  primary_capability:
    description: kubens primary capability
    patterns:
      execute:
        description: Execute kubens command
        typical_use_cases:
        - use kubens
        - kubens command
        - switch between kubernetes namespaces
        time_estimate_ms: '1000'
        cost_estimate: '2'
        complexity_score: 0.3
        scope: single_item
        completeness: complete
        policy:
          max_cost: 10
          requires_approval: false
          production_safe: true
          max_execution_time: 60
        preference_match:
          speed: 0.8
          accuracy: 0.9
          cost: 0.9
          complexity: 0.8
          completeness: 0.9
        required_inputs:
        - name: host
          type: string
          description: Target host
          validation: .*
metadata:
  author: OpsConductor Team
  created: '2025-10-03'
  updated: '2025-10-03'
  tags:
  - kubernetes
  - container
  - kubens
  documentation_url: https://docs.opsconductor.io/tools/kubens

# === EXAMPLES ===
examples:
  - name: "Execute"
    description: "Execute kubens command"
    inputs:
      host: "192.168.1.100"
    expected_time_ms: 1000
    expected_cost: 2
