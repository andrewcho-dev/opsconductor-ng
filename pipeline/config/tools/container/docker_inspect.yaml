# docker-inspect - Display detailed container information
tool_name: docker-inspect
version: '1.0'
description: Display detailed container information
platform: container
category: container
defaults:
  accuracy_level: real-time
  freshness: live
  data_source: direct
capabilities:
  primary_capability:
    description: docker-inspect primary capability
    patterns:
      execute:
        description: Execute docker-inspect command
        typical_use_cases:
        - use docker-inspect
        - docker-inspect command
        - display detailed container information
        time_estimate_ms: '1000'
        cost_estimate: '2'
        complexity_score: 0.3
        scope: single_item
        completeness: complete
        policy:
          max_cost: 10
          requires_approval: false
          production_safe: true
          max_execution_time: 60
        preference_match:
          speed: 0.8
          accuracy: 0.9
          cost: 0.9
          complexity: 0.8
          completeness: 0.9
        required_inputs:
        - name: host
          type: string
          description: Target host
          validation: .*
metadata:
  author: OpsConductor Team
  created: '2025-10-03'
  updated: '2025-10-03'
  tags:
  - container
  - container
  - docker-inspect
  documentation_url: https://docs.opsconductor.io/tools/docker-inspect

# === EXAMPLES ===
examples:
  - name: "Execute"
    description: "Execute docker-inspect command"
    inputs:
      host: "192.168.1.100"
    expected_time_ms: 1000
    expected_cost: 2
