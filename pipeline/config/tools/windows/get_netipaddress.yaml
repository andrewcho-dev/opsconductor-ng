# Get-NetIPAddress - Query IP address configuration
tool_name: Get-NetIPAddress
version: '1.0'
description: Query IP address configuration
platform: windows
category: network
defaults:
  accuracy_level: real-time
  freshness: live
  data_source: direct
capabilities:
  network_info:
    description: network_info capability
    patterns:
      query_ip:
        description: query_ip pattern
        typical_use_cases:
        - get ip address
        - network config
        - ip info
        time_estimate_ms: '300'
        cost_estimate: '1'
        complexity_score: 0.1
        scope: single_item
        completeness: complete
        policy:
          max_cost: 10
          requires_approval: false
          production_safe: true
          max_execution_time: 60
        preference_match:
          speed: 0.8
          accuracy: 0.9
          cost: 0.9
          complexity: 0.8
          completeness: 0.9
        required_inputs:
        - name: host
          type: string
          description: Target host
          validation: .*
metadata:
  author: OpsConductor Team
  created: '2025-10-03'
  updated: '2025-10-03'
  tags:
  - windows
  - network
  - get-netipaddress
  documentation_url: https://docs.opsconductor.io/tools/get-netipaddress
