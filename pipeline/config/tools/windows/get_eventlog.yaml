# Get-EventLog - Query Windows Event Logs
tool_name: Get-EventLog
version: '1.0'
description: Query Windows Event Logs
platform: windows
category: monitoring
defaults:
  accuracy_level: real-time
  freshness: live
  data_source: direct
capabilities:
  log_analysis:
    description: log_analysis capability
    patterns:
      query_events:
        description: query_events pattern
        typical_use_cases:
        - check event log
        - search events
        - get log entries
        time_estimate_ms: 1000 + 10 * N
        cost_estimate: ceil(N / 100)
        complexity_score: 0.3
        scope: single_item
        completeness: complete
        policy:
          max_cost: 10
          requires_approval: false
          production_safe: true
          max_execution_time: 60
        preference_match:
          speed: 0.8
          accuracy: 0.9
          cost: 0.9
          complexity: 0.8
          completeness: 0.9
        required_inputs:
        - name: host
          type: string
          description: Target host
          validation: .*
metadata:
  author: OpsConductor Team
  created: '2025-10-03'
  updated: '2025-10-03'
  tags:
  - windows
  - monitoring
  - get-eventlog
  documentation_url: https://docs.opsconductor.io/tools/get-eventlog

# === EXAMPLES ===
examples:
  - name: "Query Events"
    description: "query_events pattern"
    inputs:
      host: "192.168.1.100"
    expected_time_ms: 1000
    expected_cost: 1
